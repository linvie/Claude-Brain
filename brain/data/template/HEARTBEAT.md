# Heartbeat 用户自定义检查

<!-- 在此添加你希望心跳定期检查的内容。示例：

### 监控特定项目构建状态
检查 ~/code/myapp/ 目录下 `npm run build` 是否能成功执行。

### 检查 API 可用性
curl https://api.example.com/health 确认返回 200。

-->

<!-- 提示：此文件由用户维护，Brain 模板更新不会覆盖。留空则心跳只执行系统内建检查。 -->
